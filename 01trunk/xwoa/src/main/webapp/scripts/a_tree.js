function TreeUtils() {}
TreeUtils.sortUnits = function(jsonArray) {
	var 
	
	var parentNodes = new Array
	
	var root = {childNodes: new Array()};
	for (var i=0, len=jsonArray.length; i<len; i++) {
		
	}
};